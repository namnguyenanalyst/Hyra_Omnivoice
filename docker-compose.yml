version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: omnivoice_redis
    ports:
      - "6379:6379"
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - ./redis_data:/data
